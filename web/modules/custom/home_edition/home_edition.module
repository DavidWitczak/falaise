<?php

/**
 * @file
 * Contains home_edition.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function home_edition_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the home_edition module.
    case 'help.page.home_edition':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Accueil des éditions') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function home_edition_theme() {
  return [
    'home_edition' => [
      'variables' => [
        'var' => [],
      ],
    ],
  ];
}
