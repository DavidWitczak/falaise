<?php

/**
 * @file
 * Contains home_ateliers.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function home_ateliers_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the home_ateliers module.
    case 'help.page.home_ateliers':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Accueil des ateliers') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function home_ateliers_theme() {
  return [
    'home_ateliers' => [
      'variables' => [
        'var' => [],
      ],
    ],
  ];
}
