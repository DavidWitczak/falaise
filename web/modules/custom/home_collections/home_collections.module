<?php

/**
 * @file
 * Contains home_collections.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function home_collections_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the home_collections module.
    case 'help.page.home_collections':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Accueil des collections') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function home_collections_theme() {
  return [
    'home_collections' => [
      'variables' => [
        'var' => [],
      ],
    ],
  ];
}
