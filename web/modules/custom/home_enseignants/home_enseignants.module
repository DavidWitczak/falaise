<?php

/**
 * @file
 * Contains home_enseignants.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function home_enseignants_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the home_enseignants module.
    case 'help.page.home_enseignants':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Accueil enseignants') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function home_enseignants_theme() {
  return [
    'home_enseignants' => [
      'variables' => [
        'var' => [],
      ],
    ],
  ];
}
