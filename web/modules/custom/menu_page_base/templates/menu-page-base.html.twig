{% if var.page or var.main_page %}
  <div class="container mb-5">
    <div class="row">

      <div class="col-md-6 col-lg-3 mb-1">
        {% set media = getMediaInfos(var.main_page.media_id, 'carre_crop') %}

        {% if var.current_id == var.main_page_id %}
          {% set class_link = 'txt-red' %}
          {% set div_color = '<div class="div_color"></div>' %}
        {% else %}
          {% set class_link = 'txt-black' %}
          {% set div_color = '' %}
        {% endif %}

        <a href=" {{ var.main_page.url }} ">
          <div style="position: relative">
            <img class="w-100 d-none d-sm-none d-md-block" title="{{ media.name }}" alt="{{ media.name }}" src="{{ media.url }}">
            {{ div_color|raw }}
          </div>
          <div class="row mx-0 align-items-center mt-2">
            <div class="col px-0">
              <h5 class="{{ class_link }} font-weight-bold text-uppercase">{{ var.main_page.title }}</h5>
            </div>
          </div>
        </a>
      </div>

      {% for node in var.page %}
        {% set media = getMediaInfos(node.media_id, 'carre_crop') %}

        {% if var.current_id == node.current_page_liee_id %}
          {% set class_link = 'txt-red' %}
          {% set div_color = '<div class="div_color"></div>' %}
        {% else %}
          {% set class_link = 'txt-black' %}
          {% set div_color = '' %}
        {% endif %}

        <div class="col-md-6 col-lg-3 mb-1">
          <a href=" {{ node.url }} ">
            <div style="position: relative">
              <img class="w-100 d-none d-sm-none d-md-block" title="{{ media.name }}" alt="{{ media.name }}" src="{{ media.url }}">
              {{ div_color|raw }}
            </div>
            <div class="row mx-0 align-items-center mt-2">
              <div class="col px-0">
                <h5 class="{{ class_link }} font-weight-bold text-uppercase">{{ node.title }}</h5>
              </div>
            </div>
          </a>
        </div>
      {% endfor %}
    </div>
  </div>
{% endif %}
