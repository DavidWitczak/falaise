<?php

/**
 * @file
 * Contains home_pratiquer.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function home_pratiquer_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the home_pratiquer module.
    case 'help.page.home_pratiquer':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Accueil pratiquer') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function home_pratiquer_theme() {
  return [
    'home_pratiquer' => [
      'variables' => [
        'var' => [],
      ],
    ],
  ];
}
